(function(){
'use strict';
const R=6371000;const state=new Map();
function rad(v){return v*Math.PI/180}function dist(a,b,c,d){const p1=rad(a),p2=rad(c),dp=rad(c-a),dl=rad(d-b),x=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 2*R*Math.atan2(Math.sqrt(x),Math.sqrt(1-x))}
async function geocode(address){if(!address)return null;const k='geo:'+address.toLowerCase();try{const x=JSON.parse(sessionStorage.getItem(k)||'null');if(x)return x}catch{}try{const r=await fetch('https://nominatim.openstreetmap.org/search?format=json&limit=1&q='+encodeURIComponent(address),{headers:{Accept:'application/json'}}),d=await r.json();if(!d?.[0])return null;const x={lat:+d[0].lat,lon:+d[0].lon};sessionStorage.setItem(k,JSON.stringify(x));return x}catch{return null}}
function minutesTo(time){if(!time)return null;const [h,m]=time.split(':').map(Number),n=new Date(),t=new Date();t.setHours(h,m||0,0,0);return Math.round((t-n)/60000)}
function todaysJob(){const td=new Date().toISOString().slice(0,10);return(db.jobs||[]).filter(j=>j.date===td&&!['Complete','Cancelled'].includes(j.status)&&j.address).sort((a,b)=>(a.time||'99:99').localeCompare(b.time||'99:99'))[0]||null}
function customerFor(job){return(db.customers||[]).find(c=>c.company===job.company&&c.name===job.customer)||null}
function companyFor(job){return(db.companies||[]).find(c=>c.id===job.company)||null}
function draft(kind,job,eta){const cust=customerFor(job),co=companyFor(job);if(!cust?.phone)return;const first=String(cust.name||'there').split(/\s+/)[0],etaTime=new Date(Date.now()+eta*60000).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'});let msg;if(kind==='late')msg=`Hi ${first}, this is ${co?.name||'our team'}. We wanted to keep you updated — your technician is currently expected around ${etaTime}. We appreciate your patience and will keep you posted if anything changes.`;else msg=`Hi ${first}, this is ${co?.name||'our team'}. Quick update: your technician is on the way and is currently expected around ${etaTime}.`;if(window.officeSmsCompose)window.officeSmsCompose({phone:cust.phone,companyRef:job.company,title:kind==='late'?'Schedule Update':'ETA Update',message:msg})}
window.officeEvaluateFieldPrediction=async function(pos,session){if(!session||session.status!=='en_route'||!pos?.coords)return;const job=todaysJob();if(!job)return;const dest=await geocode(job.address);if(!dest)return;const meters=dist(pos.coords.latitude,pos.coords.longitude,dest.lat,dest.lon),prev=state.get(session.id),now=Date.now();let speed=null;if(prev&&now>prev.at){const moved=Math.max(0,prev.meters-meters),secs=(now-prev.at)/1000;if(moved>10)speed=moved/secs}state.set(session.id,{meters,at:now,speed:speed||prev?.speed||null});const usable=speed||prev?.speed;if(!usable||usable<1.5)return;const eta=Math.max(1,Math.round(meters/usable/60)),until=minutesTo(job.time);session.predicted_eta_minutes=eta;if(until!==null&&eta>until+10&&!session._lateDrafted){session._lateDrafted=true;draft('late',job,eta);try{logActivity(job.company,`OfficeOS predicted a delay for ${job.customer||job.title||'job'} · ETA about ${eta} min`);save()}catch{}}else if(!session._etaDrafted&&eta<=45){session._etaDrafted=true;draft('eta',job,eta)}};
})();