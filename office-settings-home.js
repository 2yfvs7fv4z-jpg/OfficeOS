(function(){
'use strict';
function el(id){return document.getElementById(id)}
function activate(pageId,btn){document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));document.querySelectorAll('nav button').forEach(b=>b.classList.remove('active'));el(pageId)?.classList.add('active');btn?.classList.add('active');if(pageId==='settings')window.renderSettingsPage?.();window.refreshOfficeOrganization?.();window.scrollTo({top:0,behavior:'auto'})}
function ensureSettingsPage(){let page=el('settings');if(page)return page;const main=document.querySelector('main');if(!main)return null;page=document.createElement('section');page.id='settings';page.className='page';page.innerHTML='<div class="row between"><div><h1>Settings</h1><div class="muted">Control OfficeOS, AI, workflows, security and business defaults.</div></div></div><div id="settingsHome"></div>';main.appendChild(page);return page}
function ensureSettingsVisible(){const page=ensureSettingsPage(),nav=document.querySelector('nav');if(!page||!nav)return false;let btn=nav.querySelector('button[data-page="settings"]');if(!btn){btn=document.createElement('button');btn.dataset.page='settings';btn.innerHTML='<span class="icon">⚙</span>Settings';nav.appendChild(btn)}if(!btn.dataset.officeBound){btn.dataset.officeBound='1';btn.addEventListener('click',e=>{e.preventDefault();activate('settings',btn)})}nav.style.gridTemplateColumns='repeat(6,minmax(0,1fr))';window.renderSettingsPage?.();return true}
function loadPayroll(){if(document.querySelector('script[data-office-payroll]'))return;const s=document.createElement('script');s.src='payroll.js?v=2';s.dataset.officePayroll='1';document.body.appendChild(s)}
let tries=0;const timer=setInterval(()=>{tries++;if(ensureSettingsVisible()||tries>40){if(tries>40||el('settings'))clearInterval(timer)}},150);setTimeout(loadPayroll,700);const baseRender=window.render;if(typeof baseRender==='function')window.render=function(){const r=baseRender.apply(this,arguments);setTimeout(ensureSettingsVisible,0);return r};window.addEventListener('pageshow',()=>setTimeout(ensureSettingsVisible,0));document.addEventListener('visibilitychange',()=>{if(!document.hidden)setTimeout(ensureSettingsVisible,0)});setTimeout(ensureSettingsVisible,50);
})();