(function(){
'use strict';
function selected(){return typeof current==='function'&&current()!=='all'?company():null}
function url(){return selected()?.playbook?.customerRequestUrl||''}
function pickCustomer(){const cid=selected()?.id;if(!cid)return null;const recent=(db.leads||[]).filter(x=>x.company===cid&&!['Won','Lost'].includes(x.status)).sort((a,b)=>String(b.updatedAt||b.createdAt||'').localeCompare(String(a.updatedAt||a.createdAt||'')))[0];if(recent)return recent;return(db.customers||[]).find(x=>x.company===cid)||null}
window.officeTextRequestLink=function(){const c=selected(),u=url(),cust=pickCustomer();if(!c)return alert('Select one company first.');if(!u)return alert('Create the customer request link first.');if(typeof window.officeSmsCompose!=='function')return alert('OfficeOS texting is still loading.');const message=window.officePlaybookMessage?.('request',{company:c,customer:cust||{},requestLink:u})||`Hi ${String(cust?.name||'').split(/\s+/)[0]||'there'}, thanks for contacting ${c.name}. Please use this secure link to tell us about your project: ${u}`;window.officeSmsCompose({companyRef:c.id,title:'Text Request Link',to:cust?.phone||'',name:cust?.name||'',message})}
function decorate(){const card=document.getElementById('customerRequestSettings');if(!card||!url()||card.querySelector('[data-text-request-link]'))return;const copy=[...card.querySelectorAll('button')].find(b=>(b.textContent||'').trim()==='Copy Link');if(!copy)return;const b=document.createElement('button');b.type='button';b.className='secondary';b.dataset.textRequestLink='1';b.textContent='Text Link';b.onclick=window.officeTextRequestLink;copy.insertAdjacentElement('afterend',b)}
const obs=new MutationObserver(()=>decorate());function start(){decorate();obs.observe(document.body,{childList:true,subtree:true});document.getElementById('companySelect')?.addEventListener('change',()=>setTimeout(decorate,100))}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
})();